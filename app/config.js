// Este arquivo serve para configurar a página raiz como dinâmica
// para evitar problemas com renderização estática em hooks client-side

export const dynamic = 'force-dynamic';
export const dynamicParams = true;
export const revalidate = 0; 